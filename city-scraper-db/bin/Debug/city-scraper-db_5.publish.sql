/*
Deployment script for CityScraperDb

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "CityScraperDb"
:setvar DefaultFilePrefix "CityScraperDb"
:setvar DefaultDataPath "C:\Program Files\Microsoft SQL Server\MSSQL14.SQLEXPRESS\MSSQL\DATA\"
:setvar DefaultLogPath "C:\Program Files\Microsoft SQL Server\MSSQL14.SQLEXPRESS\MSSQL\DATA\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
PRINT N'Creating [dbo].[AwayRecord]...';


GO
CREATE VIEW [dbo].[AwayRecord]
	AS
	select s.name, AwayTeamId, Sum(case when HomeTeamScore < AwayTeamScore then 1 else 0 end) 'Wins', Sum(case when HomeTeamScore = AwayTeamScore then 1 else 0 end) 'Ties', Sum(case when HomeTeamScore > AwayTeamScore then 1 else 0 end) 'Losses',Count(*) 'Total' from Game as g
	inner join team as t on g.AwayTeamId = t.Id
	inner join school as s on t.SchoolId = s.Id
	group by 
	AwayTeamId,
	s.name
GO
PRINT N'Creating [dbo].[HomeRecord]...';


GO
CREATE VIEW [dbo].[HomeRecord]
	AS 
	select s.name, HomeTeamId, Sum(case when HomeTeamScore > AwayTeamScore then 1 else 0 end) 'Wins', Sum(case when HomeTeamScore = AwayTeamScore then 1 else 0 end) 'Ties', Sum(case when HomeTeamScore < AwayTeamScore then 1 else 0 end) 'Losses',Count(*) 'Total' from Game as g
	inner join team as t on g.HomeTeamId = t.Id
	inner join school as s on t.SchoolId = s.Id
	group by 
	HomeTeamId,
	s.name
GO
PRINT N'Update complete.';


GO
